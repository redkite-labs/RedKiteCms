{# block body %}
<div class="container">
{% for imagesRow in images %}
    <div class="row">
    {% for image in imagesRow %}
        <div class="col-sm-6 col-md-2">
            {% set attributes = "" %}
            {% for name, value in image %}
            {% set attributeName = 'data-' ~ form_name ~ '_' ~name %}
            {% set attributes = attributes ~ attributeName ~ '=' ~ value|escape('url') ~ ' ' %}
            {% endfor %}

            <a href="#" class="thumbnail al_img" {{ attributes }}>
                {% include "RedKiteCmsBaseBlocksBundle:Content:Image/image.html.twig" with {"image" : image } %}
            </a>
        </div>  
    {% endfor %}
    </div>
{% endfor %}
</div>
{% endblock #}

{% block body %}
<div id="al_pages_list1">
    <table class="table table-condensed table-hover">
        <tbody>
            <thead>
                <th></th>
                <th>{{ "pages_controller_label_page_name"|trans({}, 'RedKiteCmsBundle', cms_language) }}</th>
                <th colspan="2"></th>
            </thead>
            {% for image in images %}
            <tr>
                <td>{% if (image.src != "") %}{{ image.src }}{% else %}{{ image.data_src }}{% endif %}</td>
                {#
                {% if active_page is defined and page == active_page %}
                <td width="85%"><a href="#" class="al_element_selector active-page {{ class }}" id="rk_page_{{ key }}" data-page-id="{{ key }}">{{ page }}</a></td>
                {% else %}
                <td width="85%"><a href="#" class="al_element_selector {{ class }}" id="rk_page_{{ key }}" data-page-id="{{ key }}">{{ page }}</a></td>
                {% endif %}
                <td width="15%">
                    {% include 'RedKiteCmsBundle:Partials:_select.html.twig' with {'id' : 'rk_language_' ~ key, 'classes': 'form-control input-xxs input-mini rk-page-language', 'select_options' : languages, 'rel': key}  %}
                </td>
                <td><button class="btn btn-xs btn-danger rk-page-remover btn-mini" data-page-id="{{ key }}">{{ trash_icon|raw }}</button></td>
                #}
            </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}